<table class="table-bordered table-striped table">
<tr>
    <th>
        Stat Summary
    </th>
    <th colspan=6 >
        <%= link_to 'back to image', report_image_path(@report, @image) %>
    </th>
</tr>
<tr>
    <th>Stat ID</th>
    <th>Stat Metric ID</th>
    <th>Stat Metric Title</th>
    <th>Image ID</th>
    <th>Report ID</th>
    <th>Report Title</th>
</tr>

<tr>
    <td><%= @stat.id %></td>
    <th><%= @stat.metric.id %></th>
    <th><%= @stat.metric.title %></th>
    <td><%= @image.id %></td>
    <td><%= @report.id %></td>
    <td><%= @report.title %></td>
</tr>
</table>


<table class="table-bordered table-striped table">
    <% if @stat.has_logs? %>
    <tr>
        <th>Date</th>
        <th>Level</th>
        <th>Message</th>
    </tr>
    <% @builds.each do |item| %>
    <tr>
        <td><%= item.created_at.strftime('%B %d, %Y at %H:%M') %></td>
        <td><%= item.level %></td>
        <td><%= item.state %></td>
        <td><%= (item.retval||"").force_encoding('UTF-8') %></td>
        <td>
            <%= link_to 'delete build', task_build_path(@task, item), :method => :delete %>
        </td>
    </tr>
    <% end %>
    <% else %>
    <tr>
        <td colspan=5> no builds found </td>
    <tr>
    <% end %>
</table>
